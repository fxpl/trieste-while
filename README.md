# While

This feature branch is a work in progress. Use the generated
`while_trieste` binary (the `while` binary will not work
correctly). Things remaining:

- Make the `while` binary output Verona bytecode without breaking
  static analysis
- Add support for function calls
- Add support for binary connectives (normalisation)
